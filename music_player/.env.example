# Music Player Environment Configuration
# IMPORTANT: These credentials are OPTIONAL for most features
# Copy this file to .env if you want to enable user-specific features
# NEVER commit the actual .env file with real credentials to version control!

# Deezer API Credentials (OPTIONAL)
# Only needed for user-specific features like personal favorites
DEEZER_APP_ID=your_deezer_app_id
DEEZER_APP_SECRET=your_deezer_app_secret 